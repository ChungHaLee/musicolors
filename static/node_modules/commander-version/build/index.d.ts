import commander from "commander";
/**
 * Set the program version to `version` from the nearest package.json up from `dirname`.
 *
 * This method auto-registers the "-v, --version" flag
 * which will print the version number when passed.
 *
 * You can optionally supply the  flags and description to override the defaults.
 */
export default function (dirname: string, flags?: string | undefined, description?: string | undefined): commander.Command;
declare const createCommand: (name?: string | undefined) => commander.Command;
export { commander, createCommand };
